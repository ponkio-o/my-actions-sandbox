post:
  hello:
    embedded_var_names:
      - env
    template: |
      hello world!!!!!!!!!!!!!! ({{.Vars.env}})

hide:
  hello: "Comment.HasMeta && (Comment.Meta.SHA1 != Commit.SHA1 && Comment.Meta.Vars.env == Vars.env)"
