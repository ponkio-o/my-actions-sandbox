name: test job

on:
  pull_request:

jobs:
  test:
    name: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: echo author
        run: echo {{ github.event.pull_request.user.login }}
